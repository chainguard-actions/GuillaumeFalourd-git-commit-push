name: Test basic commit and push

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/act-harness
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up local bare repo as remote
        run: |
          # Create a bare repo to act as the remote
          git init --bare /tmp/remote-repo.git
          git remote remove origin 2>/dev/null || true
          git remote add origin /tmp/remote-repo.git
          # Push current state to the bare repo so it has the branch
          git push origin act-harness

      - name: Create a file change to commit
        run: |
          echo "test-$(date +%s)" > tests/test-artifact.txt

      - id: action
        uses: .
        with:
          email: test@example.com
          name: Test User
          commit_message: "Test commit from act"
          target_branch: act-harness
          files: tests/test-artifact.txt
          remote_repository: origin
          access_token: fake-token
          force: "0"
          empty: "0"

      - name: Assert commit was made
        run: |
          LAST_MSG=$(git log -1 --pretty=%s)
          echo "Last commit message: $LAST_MSG"
          if [ "$LAST_MSG" != "Test commit from act" ]; then
            echo "FAIL: expected commit message 'Test commit from act' but got '$LAST_MSG'"
            exit 1
          fi
          echo "PASS"

      - name: Assert file was committed
        run: |
          git show HEAD --name-only | grep "tests/test-artifact.txt"
          if [ $? -ne 0 ]; then
            echo "FAIL: expected tests/test-artifact.txt in last commit"
            exit 1
          fi
          echo "PASS"
